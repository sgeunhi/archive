#ifndef ENUM_H
#define ENUM_H


enum Weight {
  LOW, MEDIUM, HIGH
};

enum ItemType {
    SLEEPING_BAG,
    TENT,
    LURE,
    CLOTHING,
    FISHING_ROD,
    COOKING,
    WATER
};

enum DaysOnCamp {
    ONE, TWO, THREE
};

enum MealType {
    BREAKFAST, LUNCH, DINNER, SNACK
};

#endif